import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miApp-1657956',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
